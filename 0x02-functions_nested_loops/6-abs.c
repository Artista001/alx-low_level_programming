#include "main.h"
/**
 * _abs - computes the abolute value of an integer
 * @n: The character to be checked
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
